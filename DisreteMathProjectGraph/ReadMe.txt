========================================================================
    CONSOLE APPLICATION : DisreteMathProjectGraph Project Overview
========================================================================

In this program, we created an adjacency graph of linked list array. The sized of the linked list is the number of vertices. 
The graph is programmed as a weighted graph; the weights of edges can be stored in nodes of linked lists.
At the end the program will display the degree of each time edge appears.

